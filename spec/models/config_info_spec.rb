require 'spec_helper'

describe ConfigInfo do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ConfigInfo.create!(@valid_attributes)
  end
end
