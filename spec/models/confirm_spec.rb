require 'spec_helper'

describe Confirm do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Confirm.create!(@valid_attributes)
  end
end
