require 'spec_helper'

describe TkInfo do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    TkInfo.create!(@valid_attributes)
  end
end
