require 'spec_helper'

describe Deliver do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Deliver.create!(@valid_attributes)
  end
end
