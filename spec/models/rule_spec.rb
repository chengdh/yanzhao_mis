require 'spec_helper'

describe Rule do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Rule.create!(@valid_attributes)
  end
end
