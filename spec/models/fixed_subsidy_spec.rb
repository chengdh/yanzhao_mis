require 'spec_helper'

describe FixedSubsidy do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    FixedSubsidy.create!(@valid_attributes)
  end
end
