require 'spec_helper'

describe ClearInfo do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ClearInfo.create!(@valid_attributes)
  end
end
