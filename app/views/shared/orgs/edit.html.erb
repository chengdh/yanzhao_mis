<div class="block">
  <div class="secondary-navigation">
    <%= render :partial => 'shared/orgs/caption' %>
  </div>          
  <div class="content">            
    <h2 class="title">修改</h2>
    <div class="inner">
      <% org = instance_variable_get("@#{@param_name}")%>
      <% form_for org, :html => { :class => :uniForm } do |f| %>
        <%= render "shared/orgs/form", :f => f %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, render(:partial => 'shared/orgs/sidebar') -%>
