<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
	    <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", sub_companies_path  %></li>
  		<li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_sub_company_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> SubCompanies</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
										<th>
					<%= t("activerecord.attributes.sub_company.name", :default => t("activerecord.labels.name", :default => "Name")) %>
</th>
					          <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @sub_companies.each do |sub_company| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= sub_company.id %>
          </td>
										<td>
						<%= link_to sub_company.name, sub_company_path(sub_company) %>
					</td>
										<td>
            <%= sub_company.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", sub_company_path(sub_company) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_sub_company_path(sub_company) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", sub_company_path(sub_company), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>